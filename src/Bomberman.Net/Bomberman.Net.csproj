<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <!-- Week 3: the net layer now needs the engine's input/session types (IInputState, InputState,
       GameSession) to serialize inputs and drive lockstep. It depends on the Core interface, not on
       MonoGame directly (that comes transitively via Core). -->
  <ItemGroup>
    <ProjectReference Include="..\Bomberman.Core\Bomberman.Core.csproj" />
  </ItemGroup>
</Project>
